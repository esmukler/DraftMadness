<%= render 'header', league: @league %>

<h3 class="text-center">Welcome to the Draft Room</h3>

<% if @league.full? %>
  <div class="draft-room-container" data-league-id="<%= @league.id %>">
  </div>
<% else %>
  <h4 class="text-center">
    The draft has not yet begun because your league still does not have the required 8 owners.
  </h4>
<% end %>
